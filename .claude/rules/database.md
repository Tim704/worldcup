# Database Rules
- Use connection pooling for all backend database interactions.
- All migrations must be reversible.
- Optimize queries to prevent N+1 issues when fetching user prediction leaderboards.
